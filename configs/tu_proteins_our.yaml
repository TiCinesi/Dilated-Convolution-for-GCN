out_dir: tmp_results
num_workers: 16
metric_best: 'accuracy'
metric_agg: 'argmax'
dataset:
  format: TU
  name: PROTEINS
  task: graph
  task_type: classification
  node_encoder: false
  node_encoder_bn: false
  node_encoder_name: Atom
  edge_encoder: false
  edge_encoder_bn: false
  edge_encoder_name: Bond
  dir: /cluster/scratch/piattigi/datasets
train:
  batch_size: 32
  eval_period: 1
  ckpt_period: 100
  sampler: full_batch
model:
  type: standard_gnn #[standard_gnn, dilated_gnn]
  loss_fun: cross_entropy
  graph_pooling: mean #[concat_across_sum_of_layers, mean]
gnn:
  layers_pre_mp: 0 
  layers_mp: 5 #when model=dilated_gnn not used, but needs to be > 0
  layers_k1: 2 #only model=dilated_gnn
  layers_k2: 2 #only model=dilated_gnn
  dilated_path_join: concat #[concat, add, none] #only model=dilated_gnn
  learn_alpha_residual_connection: true #[true, false] #only model=dilated_gnn
  layer_type: ginconv #[egatconv, eginconv, gatconv, ginconv]
  use_edge_features: false 
  layers_post_mp: 1
  dim_inner: 32 #hidden dim
  batchnorm: true
  act: relu #activation function
  act_on_last_layer_mp: false
  dropout: 0.5
  agg: none
  normalize_adj: false
optim:
  optimizer: adam
  base_lr: 0.01
  max_epoch: 1000
  scheduler: step_lr_epochs #step_lr_epochs
  weight_decay: 0.0 
  step_size: 50
  step_gamma: 0.5
